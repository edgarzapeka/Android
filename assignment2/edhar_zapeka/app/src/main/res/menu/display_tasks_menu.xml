<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
    android:id="@+id/add_menu"
    android:title="@string/add_menu_string"
    android:icon="@drawable/ic_add_menu"
    app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/delete_menu"
        android:title="@string/delete_menu_string"
        android:icon="@drawable/ic_delete_menu"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/settings_add_menu"
        android:title="@string/settings_string"
        app:showAsAction="never"/>
</menu>